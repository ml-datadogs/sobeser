# Помощник для собесов

Сервис позволяет дать кандидату задачи на использование бд и апи.

# Как запустить

1. Создать `secrets/secrets.json` такого содержания:
    ```json
    {
      "KEY": "VALUE"
    }
    ```
